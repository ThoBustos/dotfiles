# Files to ignore (don't apply to home directory)
LICENSE
README.md

# Private SSH hosts (VPS IPs, internal aliases) - manage manually
dot_ssh/private_config


# ----------------------------
# macOS-only files (ignore on Linux/Windows)
# ----------------------------
{{ if ne .chezmoi.os "darwin" }}
Brewfile
{{ end }}
