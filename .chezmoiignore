# Files to ignore (don't apply to home directory)
LICENSE
README.md
.gitignore

# ----------------------------
# macOS-only files (ignore on Linux/Windows)
# ----------------------------
{{ if ne .chezmoi.os "darwin" }}
Brewfile
{{ end }}

# ----------------------------
# Personal terminal/editor configs (skip when isPersonal is false,
# e.g. a headless VPS you won't code on directly)
# .chezmoiignore matches target (destination) paths, not source
# directory names — must be .config/x, not dot_config/x.
# ----------------------------
{{ if not .isPersonal }}
.config/tmux
.config/nvim
.config/zed
.config/ghostty
{{ end }}
